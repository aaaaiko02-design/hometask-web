# 端末間の同期（パソコン ⇔ スマホ）セットアップ

このアプリはサーバーを持たない静的サイト（GitHub Pages）なので、
無料の **Firebase Firestore** を使ってスマホとパソコンのタスクを自動同期します。

セットアップは一度だけ。所要 5〜10 分です。

---

## 1. Firebase プロジェクトを作る

1. https://console.firebase.google.com/ にGoogleアカウントでログイン
2. 「プロジェクトを追加」→ 好きな名前（例: `hometask`）で作成
   - Google アナリティクスは「オフ」でOK
3. 作成できたら、左メニューの **「構築」→「Firestore Database」** を開く
4. 「データベースを作成」→ ロケーションはそのまま → **「テストモードで開始」** を選んで作成
   - （後述の「セキュリティルール」で調整します）

## 2. Web アプリの設定値を取得する

1. プロジェクトのトップで、歯車マーク → **「プロジェクトの設定」**
2. 下の方の「マイアプリ」で **`</>`（ウェブ）** アイコンをクリック
3. アプリのニックネーム（何でもOK）を入れて登録
4. 表示される `firebaseConfig = { ... }` の中身をコピー

## 3. 設定値をアプリに貼り付ける

`firebase-config.js` を開き、`PASTE_...` の部分を手順2でコピーした値に置き換えます。

```js
export const firebaseConfig = {
  apiKey:            "AIza................",
  authDomain:        "hometask-xxxx.firebaseapp.com",
  projectId:         "hometask-xxxx",
  storageBucket:     "hometask-xxxx.appspot.com",
  messagingSenderId: "1234567890",
  appId:             "1:1234567890:web:abcdef......"
};
```

> ここに入る `apiKey` などは「公開して良い設定値」です。秘密鍵ではありません。
> そのまま GitHub にコミットして問題ありません（アクセス制御は次のルールで行います）。

書き換えたらコミット＆プッシュして、GitHub Pages に反映されるのを待ちます。

## 4. Firestore のセキュリティルール（推奨）

Firebase コンソール →「Firestore Database」→「ルール」タブを開き、
以下に置き換えて「公開」してください。

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 同期用の部屋だけ許可。合言葉（コード）を知っている人だけが読み書きできる想定。
    match /rooms/{code} {
      allow read, write: if true;
    }
  }
}
```

> この設定は「コードを知っている人なら誰でも読み書きできる」状態です。
> 個人利用なら、**推測されにくい同期コード**（アプリの「ランダムなコードを作成」ボタンで生成される6桁など）を使えば実用上十分です。
> より厳密にしたい場合は Firebase Authentication の導入を検討してください。

---

## 5. 使い方

1. アプリを開き、下の方の **「🔄 端末間の同期」** カードの「同期コードを設定」をタップ
2. パソコン側で **「ランダムなコードを作成」** → 「この端末で使う」
3. スマホ側でも同じカードを開き、**同じコードを入力** → 「この端末で使う」
4. 以降、タスクの追加・削除・完了・帰宅状態が両方の端末で自動的にそろいます

- バッジが **「同期中」** になっていれば接続OKです。
- 同期をやめたいときは、設定シートの「同期をやめる」をタップします。

## 補足・仕様

- 同じ日のデータを共有します（日付が変わるとタスクは自動リセット）。
- 競合時は「最後に書き込んだ内容」が優先されます（個人利用で1台ずつ使う前提）。
- オフライン時はこれまで通りローカルに保存され、オンライン復帰後に送信されます。
- `firebase-config.js` が未設定のあいだは、同期カードは「未設定」と表示され、
  アプリはこれまで通り単体で動作します。
